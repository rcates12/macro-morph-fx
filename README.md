# macro-morph-fx
A multi-effect plugin with 8 Scenes (snapshots of hidden parameters). A single Morph control smoothly interpolates between Scene A and Scene B. On top, you get 4 Macros that perform musical “gestures” (each macro moves multiple hidden parameters at once).
